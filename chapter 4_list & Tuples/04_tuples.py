# imutable type of data structure. We cannot change the values of tuple once it is created. It is similar to string in this aspect.

t = (1, 2, 3, 4, 5)
print(type(t))

t = (1,) # this how you declare a tuple with single element in it.
print(t)
