#Set is a collection of well defined objects

s = {34,56, 78} # This is how a set is declared

s = {} # This is an empty dict
e = set() # This is how a empty set is declared

f = {1, 5, 32, 45, 5, 5, 5} # A set doesn't store repetation of value, in this case a single five will be stored
print(f) 

